import MateriasShell from "@/components/materias/MateriasShell"

export default function MateriasLayout({ children }: { children: React.ReactNode }) {
  return <MateriasShell>{children}</MateriasShell>
}
